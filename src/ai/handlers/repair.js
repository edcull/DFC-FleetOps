// Repair/end-phase handler — Phase A: repair rolls are auto-applied; no AI input needed.
// Placeholder for Phase D (Brace/Contain decisions during repair).

export function handleRepair(state, rng, aiSide, personality, applyFn) {
  // Repair phase in DFC is fully automatic (rolls happen in finishActivation).
  // No AI input required in Phase A.
}
