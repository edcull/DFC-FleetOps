# Node server dependencies (installed via `npm install`; not committed)
node_modules/
reference/
data/
saves/

# Local environment (secrets — never commit)
.env

# Generated coverage (regenerate with `npm run coverage`)
coverage/

# Logs / OS cruft
*.log
.DS_Store
